# HTTP API (sessions, evaluation, static TTS)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Optional: override WebSocket origin only (default: same host as API with ws://)
# NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000
